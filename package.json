{
  "name": "prompt-api-sqlite",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npm run dev",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prettier": "prettier --write .",
    "deploy": "npm run prettier && npm run build && git add . && git commit -m 'Deploy' && git push"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "^3.49.2-build1",
    "browser-fs-access": "^0.37.0",
    "dompurify": "^3.2.6",
    "streaming-markdown": "^0.2.15"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "vite": "^6.3.5"
  }
}
